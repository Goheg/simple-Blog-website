<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple Blog Website</title>
    <link rel="stylesheet" href="/styles/index.css">
</head>
<body>
    <header>
        <h1>Simple Blog Website</h1>
    </header>
    <div class="container">
        <form class="blog-form" id="blogForm" action="/edit/<%= post.id %>" method="POST">
            <h2>Edit Blog</h2>
            <input type="text" name="title" id="blogTitle" value="<%= post.title %>" required>
            <textarea id="blogContent" name="content"  rows="5" required><%= post.content %></textarea>
            <button type="submit">Update Post</button>
        </form>
        <a href="/">Back to home</a>
    </div>
</body>
</html>